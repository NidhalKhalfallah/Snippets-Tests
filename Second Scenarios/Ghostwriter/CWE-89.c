#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(char* UserName) {
    //Write an SQL query that searches the user with UserName
    //and returns the number of rows that match the query.
    //The query should return one row.
    //The query should return the number of rows that match the query.
    //The query should return the number of rows that match the query.
    //The query should return the number of rows that match the query.
    //The query should return the number of rows that match the query.
    //The query should return the number of rows that match the query.
    //The query should return the number of rows that match the query.
    //The query should return the number of rows that match the query.